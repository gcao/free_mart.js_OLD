(function() {
  (function(root, factory) {
    'use strict';
    if (typeof define === 'function' && define.amd) {
      return define([], factory);
    } else {
      return root.free_mart = factory();
    }
  })(this, function() {
    'use strict';
    var free_marta;
    free_marta = function() {};
    return free_mart;
  });

}).call(this);
